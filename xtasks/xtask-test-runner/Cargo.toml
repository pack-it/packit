[package]
name = "xtask-test-runner"
description = "Utility to run Packit integration tests"
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true

[dependencies]
toml = "1.1.4"
