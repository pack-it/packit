# Packit Tests

This directory contains the Packit integration tests. The unit and integration tests are separated, because the integration tests need setup and cleanup functionality.

### Unit tests
Run the following command to run the unit tests:
```
cargo test
```

### Integration tests
Run the following command to run the integration tests:
```
cargo xtest
```